<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="utf-8">
   <meta name="viewport" content="width=device-width, initial-scale=1">
   <link href="css/style.css" rel="stylesheet" type="text/css">
   <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
   <link rel="preconnect" href="https://fonts.googleapis.com">
   <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
   <link href="https://fonts.googleapis.com/css2?family=Shadows+Into+Light&family=Thasadith:wght@400;700&display=swap"
      rel="stylesheet">
   <title>The Suite Life</title>

</head>

<nav class="navbar navbar-expand-lg fixed-top bg-info text-dark">
   <div class="container-fluid">
      <div class="container-fluid">
         <a class="navbar-brand" href="#">
            <p alt="" width="30" height="24" class="d-inline-block align-text-top">Logged In<a class="navbar-brand"
                  href="#"></a>
               <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                  aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation"><span
                     class="navbar-toggler-icon"></span></button>
         </a>
         <div class="collapse navbar-collapse " id="navbarSupportedContent">
            <ul class="navbar-nav mr-5">
               <li class="nav-item"><a class="nav-link" data-value="home" href="/views/homepage.handlebars">Home</a></li>
               <li class="nav-item"><a class="nav-link" data-value="passport" href="/views/profile.handlebars">Travel
                     Passport</a></li>
               <li class="nav-item"><a class="nav-link" data-value="booking" href="/viewstravel">Plan a Trip</a>
               </li>
               <li class="nav-item"><a class="nav-link" data-value="future" href="future.handlebars">Travel Bucket
                     List</a></li>
                     {{#if logged_in}}
      <a href="/profile">profile</a> |
                     <li class="nav-item"><a class="nav-link" data-value="passport" href="/public/js/login.js">Log In</a></li>

      <button class="no-button" id="logout">logout</button>
      {{else}}
      <a href="/login">login</a>
      {{/if}}
               <li class="nav-item"><a class="nav-link" data-value="passport" href="/public/js/logout.js">Log Out</a></li>
            </ul>
         </div>
      </div>
</nav>

<body>
   <nav>
      <!-- Conditionally render login or logout links -->
      {{#if logged_in}}
      <a href="/profile">profile</a> |
      <button class="no-button" id="logout">logout</button>
      {{else}}
      <a href="/login">login</a>
      {{/if}}
   </nav>

   {{{body}}}

   {{#if logged_in}}
   <script src="/js/logout.js"></script>
   {{else}}
   <script src="/js/login.js"></script>
   {{/if}}

</body>


{{!-- <script src="assets/api.js"></script> --}}

</html>